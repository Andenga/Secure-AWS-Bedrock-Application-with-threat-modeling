The beginner version of this is "I deployed a chatbot with IAM permissions." The non-beginner version threat-models the actual attack surface: prompt injection propagating into downstream tool calls, over-permissioned agent roles, data exfiltration through RAG retrieval, and audit logging of model invocations for incident response. Document the threats you considered and mitigated, not just the deployment.
